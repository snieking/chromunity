class topic_tag {
    key id: byte_array, topic, name;
    index timestamp;
}

class reply_to_topic_tag {
    key reply_id: text, topic_tag;
}

class tag_following {
    key user, tag: text;
}

include 'operations';
include 'queries';