class topicTag {
    key id: byte_array, topic, name;
    index timestamp;
}

class replyToTopicTag {
    key replyId: text, topicTag;
}

class tagFollowing {
    key user, tag: text;
}

include 'operations';
include 'queries';