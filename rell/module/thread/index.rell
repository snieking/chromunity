class thread {
    key id: text;
    index rootThreadId: text;
    index author: user;
    index visible: boolean;
    message: text;
    timestamp;
}

include 'operations';
include 'queries';
include '/thread/thread-rating/index';