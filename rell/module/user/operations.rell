operation registerUser(name, pubkey) {
    require(is_signer(pubkey));

    if (name == "admin") {
        val admin = create user(name, pubkey, op_context.transaction.block.block_height);
        create admin(admin);
        create userSettings(admin, "");
    } else {
        val user = create user(name, pubkey, op_context.transaction.block.block_height);
        create userSettings(user, "");
    }

    
}
