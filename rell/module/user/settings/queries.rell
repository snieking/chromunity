query getUserSettings(name) {
    return userSettings@?{ user@{ .name == name }} ( avatar = .avatar );
}

query getUserForumAvatar(name) {
    return userSettings@?{ user@{ .name == name }} ( .avatar );
}